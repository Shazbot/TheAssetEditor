<windowhandling:AssetEditorWindow
    x:Class="Editors.KitbasherEditor.Services.MissingTextureDecisionWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:windowhandling="clr-namespace:WindowHandling;assembly=Shared.Ui"
    Title="Texture Atlas Pack - Missing Textures"
    Width="900"
    MinWidth="640"
    MinHeight="360"
    ResizeMode="CanResize"
    WindowStartupLocation="CenterOwner"
    Style="{StaticResource CustomWindowStyle}">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            FontSize="15"
            FontWeight="SemiBold"
            Text="Unresolved texture dependencies were found" />

        <TextBlock
            Grid.Row="1"
            Margin="0,8,0,10"
            Text="Some meshes have unresolved secondary texture dependencies. Missing BaseColour textures are always skipped. Review the unresolved paths below."
            TextWrapping="Wrap"
            Opacity="0.75" />

        <TextBox
            x:Name="DetailsTextBox"
            Grid.Row="2"
            MinHeight="180"
            Padding="8"
            AcceptsReturn="True"
            AcceptsTab="True"
            FontFamily="Consolas"
            HorizontalScrollBarVisibility="Auto"
            IsReadOnly="True"
            TextWrapping="NoWrap"
            VerticalScrollBarVisibility="Auto" />

        <TextBlock
            Grid.Row="3"
            Margin="0,10,0,0"
            Text="Atlas available textures keeps unresolved secondary texture paths unchanged while atlasing the channels that can be resolved. Skip meshes with missing textures excludes those meshes from atlasing."
            TextWrapping="Wrap"
            Opacity="0.75" />

        <StackPanel
            Grid.Row="4"
            Margin="0,14,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Width="180"
                Margin="0,0,8,0"
                Click="AtlasAnywayButton_Click"
                Content="Atlas available textures" />
            <Button
                Width="210"
                Click="SkipAffectedButton_Click"
                Content="Skip meshes with missing textures"
                IsDefault="True" />
        </StackPanel>
    </Grid>
</windowhandling:AssetEditorWindow>
